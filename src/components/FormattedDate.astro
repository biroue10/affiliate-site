---
interface Props {
	date: Date;
	language?: 'fr' | 'en';
}

const { date, language = 'fr' } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString(language === 'en' ? 'en-US' : 'fr-FR', {
			year: 'numeric',
			month: 'long',
			day: 'numeric',
		})
	}
</time>
