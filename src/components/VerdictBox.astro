---
interface Props {
	text: string;
}

const { text } = Astro.props;
---

<section class="verdict-box" aria-labelledby="verdict-title">
	<p class="eyebrow">Notre verdict en bref</p>

	<h2 id="verdict-title">Ce qu’il faut retenir</h2>

	<p>{text}</p>
</section>
